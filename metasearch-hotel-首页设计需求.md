
## 参考网站
https://www.trivago.hk/zh-Hant-HK

## 设计需求

## 前端首页设计需求
1. 旨在设计一个酒店比价平台的首页，为用户提供方便的酒店搜索和比较功能。
2. 目前打算在首页展示以下功能：
   - 酒店搜索框
   - 涉及参考平台
   - 热门搜索酒店推荐
3. 针对国内酒店数据进行设计。
   - 酒店数据来源：国内酒店数据平台
     - 数据来源：价格会通过实时查询形式，考虑开发未能获取API调用，打算实时访问国内酒店数据平台的API获取酒店数据。
     - 目前留存部分酒店基础信息，包括酒店名称、地址、评分等。
   - 酒店数据展示：在首页展示国内酒店数据，包括酒店名称、地址、价格、评分等。
4. 加入一个向导功能，引导用户完成酒店搜索和比较流程。
   - 向导功能：用户在首页进行简单的搜索栏向导、热门搜索酒店向导
      - 搜索栏向导：引导用户输入搜索关键词，如“北京”、“上海”等。
      - 热门搜索酒店向导：展示热门搜索酒店，如“北京酒店”、“上海酒店”等。
   - 比较功能：用户可以在搜索结果中选择多个酒店进行比较，比较结果会展示在比较结果页面。
5. 登陆功能：用户可以在首页进行登陆，登陆后可以查看个人订单、收藏酒店等。
6. 注册功能：用户可以在首页进行注册，注册后可以登陆平台。
   - 注册功能：用户可以在首页进行注册，注册后可以登陆平台。




现在我的数据库中hotel_info的表有非常多的酒店数据，我现在需要你帮我更新这个表：
我看前台展示的比价结果显示携程、美团、飞猪的价格数据，以及距离市中心距离

1. 新建一张price_detail表，用于存储酒店价格详情：
   - hotel_id（酒店ID）
   - platform（平台名称）
   - price（数值范围进行随机赋值）
   - distance（每一个酒店都存在经纬度，通过经纬度距离市中心的距离）
   - rating（从用户评价获取）

2. 创建一个city表，用于存储城市信息：
   - city_id（城市ID）
   - province_id（省份ID）
   - city_name（城市名称）
   - city_latitude（城市纬度）
   - city_longitude（城市经度）


3. 接下来的话，对于price_detail表，需要做的事情：
   - 根据hotel_id表中的酒店ID，填充进去price_detail表中的hotel_id字段
   - 另外为每一个hotel_id，随机填充平台字段，平台字段为携程、美团、飞猪中的一个
   - 因为平台不同，可能存在一个酒店多个平台，所以存在多条记录，保证填充平台只能填写一个平台
   - 为每一个hotel_id，随机填充价格字段，价格字段为数值范围进行随机赋值（例如：300-2500元）
   - 为每一个hotel_id，随机填充距离字段，距离字段为每一个酒店都存在经纬度，通过经纬度距离市中心的距离
   - 为每一个hotel_id，随机填充评分字段，评分数值随机进行填充


现在的话呢？我要对那个 hotel_info 表以及这个 price detail 表进行瘦身一下。
1.首先是基于这个城市维度进行瘦身，针对hotel_info表进行瘦身，减少搜索压力
   - 如果一个城市只有<30 家酒店的话呢？那么就保持不变，如果一个城市下有>30家酒店的话呢？就进行。除以3，也就是说删除掉三分之一的酒店。
2. 在hotel_info表进行瘦身完毕之后，对于price_detail表，也需要进行瘦身。
   - 对于price_detail表，可以全部删除后，根据hotel_info表内容重新计算
   - 根据hotel_id表中的酒店ID，填充进去price_detail表中的hotel_id字段
   - 另外为每一个hotel_id，随机填充平台字段，平台字段为携程、美团、飞猪中的一个
   - 因为平台不同，可能存在一个酒店多个平台，所以存在多条记录，保证填充平台只能填写一个平台
   - 为每一个hotel_id，随机填充价格字段，价格字段为数值范围进行随机赋值（例如：300-2500元）
   - 为每一个hotel_id，随机填充距离字段，距离字段为每一个酒店都存在经纬度，通过经纬度距离市中心的距离
   - 为每一个hotel_id，随机填充评分字段，评分数值随机进行填充